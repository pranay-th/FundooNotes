[variables]
DJANGO_SETTINGS_MODULE = "fundoonotes.settings"

[phases.setup]
nixPkgs = ["python313", "gcc"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "gunicorn fundoonotes.wsgi:application --bind 0.0.0.0:$PORT --workers 2 --timeout 120"
